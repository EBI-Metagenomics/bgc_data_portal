# Web-only requirements — pure Django/API stack; no ML or science heavyweights.
# Use this file for the web/dev image to keep build times fast.
# ML packages (torch, transformers, esm, rdkit, pyhmmer, umap-learn) live in requirements-ml.txt.

Django==5.2.15
# Background tasks: django-tasks API + the Postgres-backed db backend, run by
# `manage.py db_worker` (replaces Celery + RabbitMQ + the Redis result store).
# django-tasks-db is GitHub-only (not on PyPI); pin to a tag.
django-tasks==0.12.0
django-tasks-db @ git+https://github.com/RealOrangeOne/django-tasks-db@0.12.0
gunicorn==23.0.0
uvicorn[standard]==0.30.1
psycopg[binary]==3.1.18
dj-database-url==2.1.0
pgvector==0.4.1
orjson>=3.10.3
pydantic>=2
plotly==5.22.0
tqdm==4.67.1
pyarrow>=23.0,<24
pandas
pyrodigal==3.6.3
numpy==2.0.1
# scipy.sparse powers the cached composite-Dice scoring matrix loaded by
# /query/similar-nrb/ and /query/nrb-architecture/ — both run synchronously
# in the web pod, so scipy has to ship with the web image even though it's
# nominally an "ML" dependency.
scipy==1.14.1
joblib==1.4.2
seaborn==0.13.2
requests==2.32.3
django-filter==24.3
django-matomo==0.1.6
whitenoise==6.4.0
django-debug-toolbar==5.0.1
django-csp
aiofiles
python-multipart==0.0.9
django-ninja==1.4
# Worker hot-reload in dev (db_worker --reload uses watchdog under DEBUG)
watchdog==4.0.1
biopython==1.85
# Web image installs common-core WITHOUT the [chemont] extra: the web pod never
# runs rdkit (the chemont rdkit imports are all function-local and only execute
# in the Celery worker). The worker pulls [chemont] via requirements-ml.txt.
mgnify-bgcs-common-core @ git+https://github.com/Finn-Lab/mgnify-bgcs-common-core.git@mgnify-bgcs-common-core-v1.7.0
