apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: bgc-local
spec:
  clusterIP: None  # headless — StatefulSet pods resolve by name
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
